import { IExample, IExampleRepository } from '../../interfaces/example.interface';

export const mapExampleFromModelToApi = (example: IExample): IExampleRepository => {
  const { } = example;
  return {
  };
}